meta {
  name: Insert New Customer Category
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/manage/customer-category
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

body:multipart-form {
  business_id: 1
  name: VIP
  description: Pelanggan VIP
}

settings {
  encodeUrl: true
}
