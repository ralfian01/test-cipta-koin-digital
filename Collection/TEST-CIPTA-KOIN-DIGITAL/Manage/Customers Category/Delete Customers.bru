meta {
  name: Delete Customers
  type: http
  seq: 3
}

delete {
  url: {{API_URL}}/manage/customers/2
  body: multipartForm
  auth: inherit
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

settings {
  encodeUrl: true
}
