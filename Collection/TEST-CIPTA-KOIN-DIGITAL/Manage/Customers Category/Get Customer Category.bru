meta {
  name: Get Customer Category
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/customer-category
  body: none
  auth: bearer
}

params:query {
  ~business_id: 1
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
