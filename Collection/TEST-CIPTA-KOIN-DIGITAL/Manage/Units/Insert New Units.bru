meta {
  name: Insert New Units
  type: http
  seq: 3
}

post {
  url: {{API_URL}}/manage/units
  body: json
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

body:json {
  {
      "name": "liter",
      "type": "QUANTITY",
      "description": "Satuan barang per-gram"
  }
}

settings {
  encodeUrl: true
}
