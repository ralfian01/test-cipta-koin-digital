meta {
  name: Update Units
  type: http
  seq: 2
}

put {
  url: {{API_URL}}/manage/units/2
  body: json
  auth: inherit
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

body:json {
  {
      "name": "Pcs"
  }
}

settings {
  encodeUrl: true
}
