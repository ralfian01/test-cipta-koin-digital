meta {
  name: Get Units
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/units?type=QUANTITY
  body: none
  auth: bearer
}

params:query {
  type: QUANTITY
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
