meta {
  name: Delete Inventory Items Categories
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/manage/inventory/item-categories/2
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
