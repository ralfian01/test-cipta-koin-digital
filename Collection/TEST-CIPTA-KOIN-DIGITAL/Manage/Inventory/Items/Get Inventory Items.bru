meta {
  name: Get Inventory Items
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/inventory/items/2
  body: none
  auth: bearer
}

params:query {
  ~business_id: 1
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
