meta {
  name: Insert Stock Out
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/manage/inventory/stock-out
  body: json
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

body:json {
  {
      "item_id": 2,
      "quantity": 1,
      "issued_to": "Raihan",
      "notes": "-"
  }
}

settings {
  encodeUrl: true
}
