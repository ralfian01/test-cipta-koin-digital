meta {
  name: Get Accounts
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/accounts
  body: none
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
