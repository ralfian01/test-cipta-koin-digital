meta {
  name: Insert New Taxes
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/manage/taxes
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

body:multipart-form {
  business_id: 1
  name: Tambahan
  rate: 12500
  type: FIXED
  ~description: 
  ~is_active: 1
}

settings {
  encodeUrl: true
}
