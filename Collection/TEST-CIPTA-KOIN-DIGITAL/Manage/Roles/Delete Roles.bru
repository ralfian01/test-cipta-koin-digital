meta {
  name: Delete Roles
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/manage/roles/1
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
