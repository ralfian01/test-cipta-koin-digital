meta {
  name: Update Roles
  type: http
  seq: 2
}

put {
  url: {{API_URL}}/manage/roles/1
  body: json
  auth: inherit
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

body:json {
  {
      // "code": "",
      "name": "Super Admin"
  }
}

settings {
  encodeUrl: true
}
