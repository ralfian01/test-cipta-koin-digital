meta {
  name: Get Roles
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/roles
  body: none
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
