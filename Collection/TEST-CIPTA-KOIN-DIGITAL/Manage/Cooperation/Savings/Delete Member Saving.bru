meta {
  name: Delete Member Saving
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/manage/cooperation/savings/2
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
