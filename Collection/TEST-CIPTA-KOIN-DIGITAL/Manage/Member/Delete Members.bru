meta {
  name: Delete Members
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/manage/members/1
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
