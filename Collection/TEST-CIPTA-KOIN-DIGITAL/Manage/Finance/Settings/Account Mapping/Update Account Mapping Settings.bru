meta {
  name: Update Account Mapping Settings
  type: http
  seq: 2
}

put {
  url: {{API_URL}}/manage/finance/settings/account-mapping
  body: json
  auth: bearer
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

auth:bearer {
  token: {{Bearer}}
}

body:json {
  {
      "business_id": 1,
      // "default_cash_account_id": 3,
      "default_ar_account_id": 5,
      "default_ap_account_id": 576
  }
}

settings {
  encodeUrl: true
}
