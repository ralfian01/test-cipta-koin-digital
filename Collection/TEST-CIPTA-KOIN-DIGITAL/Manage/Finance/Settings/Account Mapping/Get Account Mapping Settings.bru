meta {
  name: Get Account Mapping Settings
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/finance/settings/account-mapping?business_id=1
  body: none
  auth: bearer
}

params:query {
  business_id: 1
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
