meta {
  name: Update Account Category Mapping Settings
  type: http
  seq: 2
}

delete {
  url: {{API_URL}}/manage/finance/settings/account-category-mapping/1
  body: json
  auth: bearer
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
