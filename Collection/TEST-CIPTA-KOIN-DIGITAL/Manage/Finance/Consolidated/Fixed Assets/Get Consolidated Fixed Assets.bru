meta {
  name: Get Consolidated Fixed Assets
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/finance/consolidated/fixed-assets
  body: none
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
