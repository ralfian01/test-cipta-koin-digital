meta {
  name: Get Consolidated Contacts
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/manage/finance/consolidated/contacts
  body: none
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
