meta {
  name: Delete CoA
  type: http
  seq: 5
}

delete {
  url: {{API_URL}}/manage/finance/chart-of-accounts/2
  body: multipartForm
  auth: bearer
}

headers {
  Origin: {{Origin}}
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
