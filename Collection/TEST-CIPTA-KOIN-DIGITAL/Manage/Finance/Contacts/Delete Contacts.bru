meta {
  name: Delete Contacts
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/manage/finance/contacts/1
  body: json
  auth: bearer
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
}
