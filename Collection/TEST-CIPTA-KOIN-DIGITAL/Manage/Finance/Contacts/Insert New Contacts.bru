meta {
  name: Insert New Contacts
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/manage/finance/contacts
  body: json
  auth: bearer
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

auth:bearer {
  token: {{Bearer}}
}

body:json {
  {
      "business_id": 1,
      "contact_type": "CUSTOMER",
      "name": "Raihan"
  }
}

settings {
  encodeUrl: true
}
