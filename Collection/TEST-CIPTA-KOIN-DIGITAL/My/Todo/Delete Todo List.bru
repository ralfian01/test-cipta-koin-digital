meta {
  name: Delete Todo List
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/my/todo/1
  body: json
  auth: bearer
}

headers {
  Authorization: {{Bearer}}
  Origin: {{Origin}}
}

auth:bearer {
  token: {{Bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
